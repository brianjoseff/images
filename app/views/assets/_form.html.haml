= form_for @asset, :html => {:multipart => true} do |f|
  - if @asset.errors.any?
    #error_explanation
      %h2
        = pluralize(@asset.errors.count, "error")
        prohibited this asset from being saved:
      %ul
        - @asset.errors.full_messages.each do |msg|
          %li= msg
  =f.file_field :image
  .actions
    = f.submit
